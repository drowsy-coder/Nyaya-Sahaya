import 'package:flutter/material.dart';

class LawyerHomePage extends StatelessWidget {
  const LawyerHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Lawyer Home Page');
  }
}